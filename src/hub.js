var Hub = (function () {

  return {
    pub: function (event) {
      //
    },

    sub: function (event, handler, context) {
      //
    },

    unsub: function (event, handler) {
      //
    }
  }
}());